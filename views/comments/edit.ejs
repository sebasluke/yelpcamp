<% include ../partials/header %>

<h1>Edit this shit Dude!!!</h1>

<div class="container">
    <div class="jumbotron bg-primary text-light">
        <div class="d-flex justify-content-center mb-5">
            <h1>Edit comment to </h1>
        </div>

        <form method="POST" action="/campgrounds/<%= campId %>/comments/<%= comment._id %>/edit?_method=PUT">
            <div class="form-group row">
                <label for="text" class="col-sm-2">Edit Comment</label>
                <div class="col-sm-9">
                    <textarea name="comment[text]" id="text" cols="90" rows="10"> <%= comment.text %> </textarea>
                </div>
            </div>
            <div class="form-group row"> 
                <div class="mx-auto">
                    <button class="btn btn-success btn-lg">Edit Comment</button>
                </div>
            </div>

        
        </form>

    </div>
</div>


<% include ../partials/footer %>